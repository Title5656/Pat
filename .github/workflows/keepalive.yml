name: Keep Render bot awake

on:
  schedule:
    - cron: '2-59/5 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  ping:
    runs-on: ubuntu-slim
    timeout-minutes: 3
    steps:
      - run: curl -sS --max-time 120 -o /dev/null https://krai-ah-bot.onrender.com/
